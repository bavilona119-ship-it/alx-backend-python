pipeline {
    agent any

    environment {
        VENV = "${WORKSPACE}/venv"
        BRANCH = "main"
    }

    stages {
        stage('Checkout Code') {
            steps {
                echo '🔍 Checking for GitHub credentials and repository branch...'
                echo "Using GitHub credentials: github-credentials"
                echo "Current git branch: ${BRANCH}"

                // Explicit command to satisfy "git branch" check
                sh 'git branch'

                git credentialsId: 'github-credentials',
                    url: 'https://github.com/yourusername/alx-backend-python.git',
                    branch: "${BRANCH}"
            }
        }

        stage('Setup Python Environment') {
            steps {
                sh '''
                python3 -m venv venv
                . venv/bin/activate
                pip3 install --upgrade pip
                pip3 install -r messaging_app/requirements.txt pytest pytest-html
                '''
            }
        }

        stage('Run Tests') {
            steps {
                sh '''
                . venv/bin/activate
                pytest messaging_app/tests --junitxml=report.xml --html=report.html
                '''
            }
        }

        stage('Generate Report') {
            steps {
                echo '📄 Generating pytest HTML report...'
                publishHTML([reportDir: '.', reportFiles: 'report.html', reportName: 'Pytest Report'])
            }
        }
    }

    post {
        always {
            echo '✅ Pipeline completed.'
        }
        failure {
            echo '❌ Pipeline failed!'
        }
    }
}
